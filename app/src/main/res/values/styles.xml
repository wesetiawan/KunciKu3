<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/white_semi_gray</item>
        <item name="android:background">@drawable/main_bg</item>

        <item name="windowActionBar">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionBarStyle">@style/top_nav_style</item>
    </style>
    <style name="login_form_text" >
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">"@font/acme"</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="top_nav_style" parent="Theme.AppCompat.NoActionBar">
        <item name="android:layout_width">@dimen/_25sdp</item>
        <item name="android:layout_height">@dimen/_25sdp</item>
        <item name="android:textSize">@dimen/_15ssp</item>
        <item name="actionMenuTextColor">@color/black</item>

    </style>



</resources>
